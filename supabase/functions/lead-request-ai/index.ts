import { serve } from "https://deno.land/std@0.168.0/http/server.ts";

const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
};

serve(async (req) => {
  if (req.method === 'OPTIONS') {
    return new Response(null, { headers: corsHeaders });
  }

  try {
    const { answers } = await req.json();
    console.log('Received answers for AI recommendation:', answers);

    const LOVABLE_API_KEY = Deno.env.get('LOVABLE_API_KEY');
    if (!LOVABLE_API_KEY) {
      console.error('LOVABLE_API_KEY is not configured');
      return new Response(JSON.stringify({
        recommendation: {
          name: 'مشاوره تخصصی',
          type: 'service',
          explanation: 'با توجه به اطلاعات شما، پیشنهاد می‌کنیم با کارشناسان ما صحبت کنید.',
          sellingScript: 'کارشناسان ما آماده راهنمایی شما هستند.'
        }
      }), {
        headers: { ...corsHeaders, 'Content-Type': 'application/json' }
      });
    }

    const systemPrompt = `تو یک مشاور فروش حرفه‌ای آکادمی رفیعی هستی. بر اساس پاسخ‌های کاربر، بهترین دوره یا خدمات را پیشنهاد بده و یک متن فروش جذاب بنویس.

📚 دوره‌های رایگان (برای بودجه کم یا شروع):
- "مزه بدون مرز" - آشنایی با کار بین‌المللی (slug: maze-bedoun-marz)
- "زندگی هوشمند" - مقدمات زندگی هوشمند با AI (slug: zendegi-hooshmand)
- "مزه اینستاگرام" - آشنایی با کسب‌وکار اینستاگرام (slug: maze-instagram)
- "دوره رایگان متاورس" - مقدمات متاورس (slug: metaverse-free)
- "پروژه تغییر" - شروع تغییر در زندگی (slug: project-taghyir)
- "پروژه درآمد غیرفعال" - آشنایی با درآمد غیرفعال (slug: passive-income)
- "پروژه بیزینس آمریکایی" - آشنایی با بازار آمریکا (slug: american-business)

🎓 دوره‌های شروع (قیمت متوسط):
- "شروع بدون مرز" - شروع کار با شرکت‌های خارجی (slug: start-bedoun-marz)
- "پک هوشمند" - پکیج کامل هوش مصنوعی (slug: pack-hooshmand)
- "دوره متاورس" - آموزش جامع متاورس (slug: metaverse)
- "دوره ثروت" - آموزش مدیریت مالی (slug: servat)

💎 دوره‌های پریمیوم (قیمت بالا):
- "دوره جامع بدون مرز" - پکیج کامل کار بین‌المللی و مهاجرت کاری (slug: bedoun-marz)
- "دوره جامع مدرسه هوش مصنوعی" - آموزش کامل AI و درآمدزایی (slug: ai-school)

🛠️ خدمات:
- "ثبت شرکت بین‌المللی" - ثبت شرکت در بریتانیا، کانادا، آمریکا، دبی
- "افتتاح حساب بین‌المللی" - افتتاح حساب بانکی بین‌المللی
- "خرید سیم کارت" - سیم کارت بین‌المللی
- "افتتاح درگاه پرداخت" - درگاه پرداخت بین‌المللی مثل Stripe
- "طراحی سایت" - طراحی وبسایت حرفه‌ای
- "تولید محتوا" - تولید محتوای حرفه‌ای
- "مدیریت شبکه‌های اجتماعی" - مدیریت سوشال مدیا
- "ساخت هوش مصنوعی" - توسعه راهکارهای AI سفارشی
- "توسعه کسب‌وکار" - مشاوره و توسعه کسب‌وکار

قوانین انتخاب:
1. اگر بودجه "کمتر از ۵ میلیون" یا "۵ تا ۱۰ میلیون" است → دوره رایگان یا شروع پیشنهاد بده
2. اگر بودجه "۱۰ تا ۲۰ میلیون" است → دوره شروع پیشنهاد بده
3. اگر بودجه "بیش از ۲۰ میلیون" است → دوره پریمیوم پیشنهاد بده
4. اگر هدف مهاجرت یا کار خارجی است → دوره‌های بدون مرز پیشنهاد بده
5. اگر هدف درآمدزایی آنلاین یا AI است → دوره‌های هوش مصنوعی پیشنهاد بده
6. اگر علاقه‌مندی به خدمات خاص است → خدمات مرتبط پیشنهاد بده

پاسخ را دقیقاً به این فرمت JSON برگردان:
{
  "name": "نام دقیق دوره یا خدمات",
  "type": "course" یا "service",
  "tier": "free" یا "start" یا "premium" (فقط برای دوره‌ها),
  "slug": "slug دوره برای لینک" (فقط برای دوره‌ها، خالی برای خدمات),
  "explanation": "توضیح ۲-۳ جمله‌ای که چرا این انتخاب مناسب است",
  "sellingScript": "یک متن فروش ۳-۴ جمله‌ای جذاب و انگیزه‌بخش که کاربر را آماده خرید کند. از مزایا و نتایج صحبت کن."
}`;

    const userMessage = `اطلاعات کاربر:
- هدف: ${answers?.goal || 'مشخص نشده'}
- وضعیت فعلی: ${answers?.current_status || 'مشخص نشده'}
- علاقه‌مندی‌ها: ${answers?.interests?.join('، ') || 'مشخص نشده'}
- بودجه: ${answers?.budget || 'مشخص نشده'}

بهترین پیشنهاد را با متن فروش ارائه بده.`;

    const response = await fetch('https://ai.gateway.lovable.dev/v1/chat/completions', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${LOVABLE_API_KEY}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        model: 'google/gemini-2.5-flash',
        messages: [
          { role: 'system', content: systemPrompt },
          { role: 'user', content: userMessage }
        ],
        response_format: { type: 'json_object' }
      }),
    });

    if (!response.ok) {
      const errorText = await response.text();
      console.error('AI API error:', response.status, errorText);
      throw new Error('AI API error');
    }

    const aiResponse = await response.json();
    const content = aiResponse.choices?.[0]?.message?.content;
    
    console.log('Raw AI response:', content);
    
    let recommendation;
    try {
      recommendation = JSON.parse(content);
    } catch (e) {
      console.error('Failed to parse AI response:', content);
      recommendation = {
        name: 'مشاوره تخصصی',
        type: 'service',
        explanation: 'با توجه به اطلاعات شما، پیشنهاد می‌کنیم با کارشناسان ما صحبت کنید تا بهترین مسیر را برایتان مشخص کنیم.',
        sellingScript: 'تیم مشاوره ما آماده است تا با بررسی دقیق شرایط شما، بهترین مسیر موفقیت را طراحی کند.'
      };
    }

    console.log('AI recommendation:', recommendation);

    return new Response(JSON.stringify({ recommendation }), {
      headers: { ...corsHeaders, 'Content-Type': 'application/json' }
    });

  } catch (error) {
    console.error('Error in lead-request-ai:', error);
    return new Response(JSON.stringify({
      recommendation: {
        name: 'مشاوره تخصصی',
        type: 'service',
        explanation: 'با توجه به اطلاعات شما، پیشنهاد می‌کنیم با کارشناسان ما صحبت کنید.',
        sellingScript: 'کارشناسان ما آماده پاسخگویی و راهنمایی شما هستند. همین الان تماس بگیرید!'
      }
    }), {
      headers: { ...corsHeaders, 'Content-Type': 'application/json' }
    });
  }
});
