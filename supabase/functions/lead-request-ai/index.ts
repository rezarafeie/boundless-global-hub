import { serve } from "https://deno.land/std@0.168.0/http/server.ts";

const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
};

serve(async (req) => {
  if (req.method === 'OPTIONS') {
    return new Response(null, { headers: corsHeaders });
  }

  try {
    const { answers } = await req.json();
    console.log('Received answers for AI recommendation:', answers);

    const LOVABLE_API_KEY = Deno.env.get('LOVABLE_API_KEY');
    if (!LOVABLE_API_KEY) {
      console.error('LOVABLE_API_KEY is not configured');
      return new Response(JSON.stringify({
        recommendation: {
          courses: [],
          services: [],
          sellingScript: 'با توجه به اطلاعات شما، پیشنهاد می‌کنیم با کارشناسان ما صحبت کنید.'
        }
      }), {
        headers: { ...corsHeaders, 'Content-Type': 'application/json' }
      });
    }

    const systemPrompt = `تو یک مشاور فروش حرفه‌ای آکادمی رفیعی هستی. بر اساس پاسخ‌های کاربر، بهترین دوره‌ها و/یا خدمات را پیشنهاد بده.

مهم: اکثر اوقات بهترین پیشنهاد ترکیبی از دوره‌ها و خدمات است. مثلاً اگر کسی می‌خواد کار خارجی کنه، هم دوره بدون مرز نیاز داره و هم خدمات ثبت شرکت و درگاه پرداخت.

📚 دوره‌های رایگان (tier: free):
- "مزه بدون مرز" - آشنایی با کار بین‌المللی (slug: maze-bedoun-marz)
- "زندگی هوشمند" - مقدمات زندگی هوشمند با AI (slug: zendegi-hooshmand)
- "مزه اینستاگرام" - آشنایی با کسب‌وکار اینستاگرام (slug: maze-instagram)
- "دوره رایگان متاورس" - مقدمات متاورس (slug: metaverse-free)
- "پروژه تغییر" - شروع تغییر در زندگی (slug: project-taghyir)
- "پروژه درآمد غیرفعال" - آشنایی با درآمد غیرفعال (slug: passive-income)
- "پروژه بیزینس آمریکایی" - آشنایی با بازار آمریکا (slug: american-business)

🎓 دوره‌های شروع (tier: start):
- "شروع بدون مرز" - شروع کار با شرکت‌های خارجی (slug: start-bedoun-marz)
- "پک هوشمند" - پکیج کامل هوش مصنوعی (slug: pack-hooshmand)
- "دوره متاورس" - آموزش جامع متاورس (slug: metaverse)
- "دوره ثروت" - آموزش مدیریت مالی (slug: servat)

💎 دوره‌های پریمیوم (tier: premium):
- "دوره جامع بدون مرز" - پکیج کامل کار بین‌المللی و مهاجرت کاری (slug: bedoun-marz)
- "دوره جامع مدرسه هوش مصنوعی" - آموزش کامل AI و درآمدزایی (slug: ai-school)

🛠️ خدمات:
- "ثبت شرکت در بریتانیا" - ثبت شرکت در انگلستان
- "ثبت شرکت در کانادا" - ثبت شرکت در کانادا
- "ثبت شرکت در آمریکا" - ثبت شرکت در آمریکا (LLC, Corp)
- "ثبت شرکت در دبی" - ثبت شرکت در امارات
- "افتتاح حساب بین‌المللی" - حساب بانکی Wise, Payoneer و...
- "خرید سیم کارت بین‌المللی" - سیم کارت برای فعال‌سازی اکانت‌ها
- "افتتاح درگاه پرداخت" - Stripe, PayPal و درگاه‌های بین‌المللی
- "طراحی سایت" - طراحی وبسایت حرفه‌ای
- "تولید محتوا" - تولید محتوای ویدیویی و متنی
- "مدیریت شبکه‌های اجتماعی" - مدیریت سوشال مدیا
- "ساخت هوش مصنوعی سفارشی" - توسعه راهکارهای AI
- "مشاوره توسعه کسب‌وکار" - مشاوره و منتورینگ

قوانین پیشنهاد:
1. اگر کاربر نوع درخواست "services" انتخاب کرده → خدمات + دوره‌های مرتبط پیشنهاد بده
2. اگر کاربر نوع درخواست "learning" انتخاب کرده → دوره‌ها + خدمات مرتبط پیشنهاد بده
3. بودجه رایگان یا تا ۵ میلیون → دوره رایگان + خدمات ارزان
4. بودجه ۵ تا ۱۰ میلیون → دوره شروع + خدمات
5. بودجه بالای ۱۰ میلیون → دوره پریمیوم + خدمات کامل
6. هدف مهاجرت یا کار خارجی → دوره‌های بدون مرز + ثبت شرکت + حساب بین‌المللی
7. هدف AI یا درآمدزایی آنلاین → دوره‌های هوش مصنوعی + طراحی سایت

پاسخ را دقیقاً به این فرمت JSON برگردان:
{
  "courses": [
    {
      "name": "نام دوره",
      "tier": "free" | "start" | "premium",
      "slug": "slug برای لینک",
      "explanation": "چرا این دوره مناسب است (۱-۲ جمله)"
    }
  ],
  "services": [
    {
      "name": "نام خدمات",
      "explanation": "چرا این خدمات مناسب است (۱-۲ جمله)"
    }
  ],
  "sellingScript": "یک متن فروش ۳-۴ جمله‌ای جذاب و انگیزه‌بخش که کاربر را آماده تماس با کارشناس کند. از مزایا و نتایج ملموس صحبت کن."
}

حداقل ۱ دوره و ۱ خدمت پیشنهاد بده (مگر بودجه کاربر خیلی محدود باشه). ترکیب دوره و خدمات بهترین فروش را ایجاد می‌کند.`;

    const userMessage = `اطلاعات کاربر:
- نوع درخواست: ${answers?.request_type === 'services' ? 'خدمات' : answers?.request_type === 'learning' ? 'یادگیری' : 'مشخص نشده'}
- خدمات انتخابی: ${answers?.selected_services?.join('، ') || 'مشخص نشده'}
- هدف: ${answers?.goal || 'مشخص نشده'}
- وضعیت فعلی: ${answers?.current_status || 'مشخص نشده'}
- علاقه‌مندی‌ها: ${answers?.interests?.join('، ') || 'مشخص نشده'}
- بودجه: ${answers?.budget || 'مشخص نشده'}

بهترین ترکیب دوره‌ها و خدمات را با متن فروش ارائه بده.`;

    const response = await fetch('https://ai.gateway.lovable.dev/v1/chat/completions', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${LOVABLE_API_KEY}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        model: 'google/gemini-2.5-flash',
        messages: [
          { role: 'system', content: systemPrompt },
          { role: 'user', content: userMessage }
        ],
        response_format: { type: 'json_object' }
      }),
    });

    if (!response.ok) {
      const errorText = await response.text();
      console.error('AI API error:', response.status, errorText);
      throw new Error('AI API error');
    }

    const aiResponse = await response.json();
    const content = aiResponse.choices?.[0]?.message?.content;
    
    console.log('Raw AI response:', content);
    
    let recommendation;
    try {
      recommendation = JSON.parse(content);
    } catch (e) {
      console.error('Failed to parse AI response:', content);
      recommendation = {
        courses: [{
          name: 'دوره جامع بدون مرز',
          tier: 'premium',
          slug: 'bedoun-marz',
          explanation: 'این دوره جامع‌ترین آموزش برای شروع کار بین‌المللی است.'
        }],
        services: [{
          name: 'مشاوره تخصصی',
          explanation: 'با کارشناسان ما صحبت کنید تا بهترین مسیر را برایتان مشخص کنیم.'
        }],
        sellingScript: 'تیم مشاوره آکادمی رفیعی آماده است تا با بررسی دقیق شرایط شما، بهترین مسیر موفقیت را طراحی کند. همین الان تماس بگیرید!'
      };
    }

    console.log('AI recommendation:', recommendation);

    return new Response(JSON.stringify({ recommendation }), {
      headers: { ...corsHeaders, 'Content-Type': 'application/json' }
    });

  } catch (error) {
    console.error('Error in lead-request-ai:', error);
    return new Response(JSON.stringify({
      recommendation: {
        courses: [],
        services: [{
          name: 'مشاوره تخصصی',
          explanation: 'با توجه به اطلاعات شما، پیشنهاد می‌کنیم با کارشناسان ما صحبت کنید.'
        }],
        sellingScript: 'کارشناسان ما آماده پاسخگویی و راهنمایی شما هستند. همین الان تماس بگیرید!'
      }
    }), {
      headers: { ...corsHeaders, 'Content-Type': 'application/json' }
    });
  }
});
